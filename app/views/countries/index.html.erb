<h1>Countries#index</h1>
<%= will_paginate @countries, previous_label: "PREV", next_label: "NEXT", inner_window: 1, outer_window: 0 %>
<table>
    <tr>
      <th>Id.</th>
      <th>Country</th>
    </tr>
    <% @countries.each do |country| %>
      <tr>
        <td><%= country.id %></td>
        <td><%= country.name %></td>
      </tr>
    <% end %>
</table>
<%= will_paginate @countries, previous_label: "PREV", next_label: "NEXT", inner_window: 1, outer_window: 0 %>
